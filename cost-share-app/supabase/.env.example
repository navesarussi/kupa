# Copy to supabase/.env — used by npm run seed and schema verification only.
# Never commit supabase/.env. Service role bypasses RLS (dev scripts only).

SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR-SERVICE-ROLE-KEY
SEED_DEV_PASSWORD=your-dev-password-here
